streamlit==1.38.0
streamlit-webrtc==0.47.7
opencv-python-headless==4.10.0.84

# av causes build errors on Streamlit Cloud — use safe precompiled version
av==10.0.0

# Lightweight dependencies only
numpy
pillow
